# Copia que se publica como artifact, se regenera desde index.html
.artifact/

# Notas y capturas del proceso de diseño (pesan mucho y no hacen falta en la web)
.impeccable/

# Configuración local del entorno
.claude/

# Material original del cliente (fotos, videos y el PDF del logo)
.cliente/

# Sistema
Thumbs.db
.DS_Store

# Muestrario de animaciones, herramienta de trabajo
reel.html
